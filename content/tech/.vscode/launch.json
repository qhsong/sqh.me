{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Mock-Debug",
            "type": "mock",
            "request": "launch",
            "program": "${workspaceRoot}/${command.AskForProgramName}",
            "stopOnEntry": true
        }
    ]
}